<Application x:Class="BaiKtraTX2.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BaiKtraTX2"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="dgvHeader" TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <!--Tạo nút lệnh có hình elip-->
        <ControlTemplate x:Key="btnTemplate" TargetType="Button">
            <Grid>
                <Border Background="Gold" CornerRadius="50"/>
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </ControlTemplate>
        
        <Style x:Key="btnStyle" TargetType="Button">
            <Setter Property="Foreground" Value="Green"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template" Value="{StaticResource btnTemplate}"/>
        </Style>
    </Application.Resources>
</Application>
